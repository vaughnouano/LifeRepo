import { StyleSheet } from "react-native";

export const layout = StyleSheet.create({
  container: {
    padding: 4,
    gap: 3,

    flexDirection: "row",
    alignItems: "center",
  },

  navButton: {
    flex: 1,
    paddingVertical: 16,

    alignItems: "center",
  },
});
